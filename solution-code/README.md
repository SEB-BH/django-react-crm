# Completed solution

The completed reference application is split into two independently run projects:

- `back-end`: Django REST Framework, PostgreSQL, and Simple JWT
- `front-end`: React, Vite, React Router, and plain CSS

Follow each directory's README to install and run the application.
